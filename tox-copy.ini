[tox]
envlist = py27

[testenv]
commands =
    coverage run --branch --source=revproxy setup.py test
    coverage report --show-missing

deps =
    pip==20.1
    zipp==0.6.0
    coverage==5.1
    django==1.9
    mock<3
    lxml>=3.4
    
